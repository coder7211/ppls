---
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Brand Rapid</title>
    <style>
      body {
        margin: 0;
        font-family: "Futura BT", sans-serif;
        background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
        color: #333;
      }
      section {
        padding: 80px 20px;
        text-align: center;
      }
      h1 {
        font-size: clamp(2.5rem, 6vw, 5rem);
        margin-bottom: 0.3em;
        font-weight: bold;
        color: #111;
      }
      h2 {
        font-size: clamp(1.2rem, 3vw, 1.8rem);
        margin-bottom: 1em;
        font-weight: normal;
        color: #444;
      }
      p {
        max-width: 700px;
        margin: 0 auto;
        font-size: clamp(1rem, 2vw, 1.2rem);
        line-height: 1.6;
        color: #555;
      }
      hr {
        border: none;
        height: 1px;
        background: #ccc;
        margin: 60px auto;
        width: 80%;
      }
      .contact {
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 8px 20px rgba(0,0,0,0.05);
        padding: 40px;
        max-width: 600px;
        margin: 0 auto;
      }
      .contact h3 {
        margin-bottom: 1em;
        font-size: 1.5rem;
        color: #111;
      }
      .contact input,
      .contact textarea {
        width: 100%;
        padding: 12px;
        margin: 10px 0;
        border-radius: 8px;
        border: 1px solid #ccc;
        font-size: 1rem;
      }
      .contact button {
        margin-top: 10px;
        background: #111;
        color: #fff;
        border: none;
        padding: 12px 24px;
        border-radius: 8px;
        cursor: pointer;
        font-size: 1rem;
        transition: background 0.3s;
      }
      .contact button:hover {
        background: #444;
      }
    </style>
  </head>
  <body>
    <section>
      <h1>Brand Rapid</h1>
      <h2>Marketing</h2>
      <p>
        BrandRapid is a forward-thinking digital marketing agency that elevates brands using 
        innovative advertising solutions, committed to driving growth and engagement for every client.
      </p>
    </section>

    <hr />

    <section class="contact">
      <h3>Contact Us</h3>
      <form>
        <input type="text" placeholder="Your Name" required />
        <input type="email" placeholder="Your Email" required />
        <textarea placeholder="Your Message" rows="4" required></textarea>
        <button type="submit">Send Message</button>
      </form>
    </section>
  </body>
</html>
